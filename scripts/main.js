// ____________________________________Numbers

const alturaCm = 172;
const alturaM = 1.72;
const pesoK = 124.5;
const alturaR = Math.ceil(alturaM);
const pesoR = Math.floor(pesoK);
const maxValPlus = Number.MAX_VALUE + 1;

if (maxValPlus === Number.MAX_VALUE) {
  console.log(`Son iguales`);
}
